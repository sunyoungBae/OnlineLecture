---
id: P03-T03
title: 보호 경로·역할 가드·관리자 승격 절차
status: review
type: feature
depends_on: ["P03-T02"]
parallel_group: ""
owner: "Codex/p03_t03"
started_at: "2026-08-07T23:45:05+09:00"
blocked_reason: ""
owned_files: ["src/lib/auth/require-role.ts", "src/lib/auth/require-role.test.ts", "docs/operations/admin-bootstrap.md", "supabase/migrations/202608070005_admin_bootstrap.sql", "supabase/tests/admin_bootstrap.sql"]
shared_files: ["src/proxy.ts"]
implementation_commit: "c8a2369"
reviewer: ""
review_commit: ""
---

# 목표

보호 경로·역할 가드·관리자 승격 절차을 완료해 다음 의존 작업이 사용할 검증된 산출물을 제공한다.

# 범위

- 생성 또는 수정: src/lib/auth/require-role.ts
- 생성 또는 수정: src/lib/auth/require-role.test.ts
- 생성 또는 수정: docs/operations/admin-bootstrap.md
- 생성 또는 수정: supabase/migrations/202608070005_admin_bootstrap.sql
- 생성 또는 수정: supabase/tests/admin_bootstrap.sql
- 통합 소유 시에만 수정: src/proxy.ts

# 제외

제품 설계의 명시적 제외 기능과 이 카드 뒤 작업의 기능은 추가하지 않는다.

# 선행조건과 차단 해제

P03-T02가 모두 done이면 ready로 전환한다.

# 파일

- src/lib/auth/require-role.ts
- src/lib/auth/require-role.test.ts
- docs/operations/admin-bootstrap.md
- supabase/migrations/202608070005_admin_bootstrap.sql
- supabase/tests/admin_bootstrap.sql
- src/proxy.ts

# 인터페이스

선행 카드가 제공한 공개 타입과 동작만 소비한다. 이 카드의 파일과 검증 결과가 후속 카드의 계약이다.

# 실행 순서

1. AGENTS.md와 이 카드를 읽고 ready 여부를 확인한 뒤 owner, started_at, status를 갱신한다.
2. 인수 조건을 가장 좁게 증명하는 실패 테스트를 owned_files의 테스트 경로에 먼저 작성한다.
3. 다음 작업 전 검사를 실행한다: npm run test -- src/lib/auth/require-role.test.ts && supabase test db supabase/tests/admin_bootstrap.sql
4. 기대 결과를 확인한다: 역할 가드와 1회 관리자 승격 DB 경계가 없어 실패.
5. 범위 안의 최소 변경만 구현한다.
6. 다음 통과 검사를 실행한다: npm run test -- src/lib/auth/require-role.test.ts && supabase db reset && supabase test db && npm run typecheck
7. ./scripts/check-harness.sh와 사용 가능한 공통 완료 명령을 실행한다.
8. 카드와 대시보드를 review로 바꾸고 구현 커밋을 기록한다.
9. 구현자와 다른 리뷰어가 승인하면 reviewer, review_commit과 done 상태를 같은 커밋에 기록한다.

# 검증

- 증거 유형: RED/GREEN
- 작업 전 필수 행동: 실패 테스트를 먼저 작성한다.
- 작업 전 명령: npm run test -- src/lib/auth/require-role.test.ts && supabase test db supabase/tests/admin_bootstrap.sql
- 예상 작업 전 결과: 역할 가드와 1회 관리자 승격 DB 경계가 없어 실패
- 완료 명령: npm run test -- src/lib/auth/require-role.test.ts && supabase db reset && supabase test db && npm run typecheck
- 기대 완료 결과: 회원·운영자 가드와 1회 관리자 승격 절차가 확인된다.

# 인수 조건

- 회원·운영자 가드와 1회 관리자 승격 절차가 확인된다.
- 정상 경로와 거부 경계가 검증된다.
- 비밀 값, 범위 밖 변경과 병렬 소유권 충돌이 없다.
- 카드, 대시보드, 구현 및 리뷰 커밋이 일치한다.

# 리뷰 증거

- RED: `npm run test -- src/lib/auth/require-role.test.ts`에서 역할 가드 모듈 부재를 확인했다. 리뷰 보강 RED에서 client factory/getUser 예외 2건이 로그인으로 잘못 처리되고 page wrapper 2건이 함수 부재로 실패했다.
- DB RED 설계: 관리자 승격 migration보다 먼저 없는 대상·이미 admin·두 번째 대상·위조 metadata·권한 거부 pgTAP을 작성했다. 실제 RED 실행은 사용자 Docker 실행 경계 때문에 생략하고 migration 구현 후 전체 DB GREEN을 확인했다.
- GREEN: 역할 가드 19개 테스트가 DB profile role·인증 subject ID, member/admin 행렬, 안전한 로그인 경로, page notFound 변환과 SDK/DB 오류 전파를 검증했다.
- DB GREEN: 사용자가 로컬에서 `npx supabase --agent no db reset`과 `npx supabase --agent no test db`를 실행해 5개 파일, 161개 pgTAP 모두 통과한 `Result: PASS`를 확인했다.
- 공통 검사: `./scripts/check-harness.sh`, `npm run lint`, `npm run typecheck`, `npm run test`(96), `npm run test:e2e`(16), `npm run build`, `git diff --check`가 모두 통과했다.
- 구현 커밋: `c8a2369` (역할 가드 `cdf1b8b`, bootstrap migration·문서·pgTAP `a9fc767` 포함)
- 독립 리뷰: `Codex/p03_t03_contract`와 `Codex/p03_t03_security`가 역할 신뢰 경계, 오류·redirect, private singleton audit, 함수 권한·원자성과 19개 pgTAP 계약을 검토해 Critical/Important 없음으로 승인했다.

# 커밋

권장 메시지: 기능: 보호 경로·역할 가드·관리자 승격 절차
