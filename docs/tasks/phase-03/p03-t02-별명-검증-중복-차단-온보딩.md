---
id: P03-T02
title: 별명 검증·중복 차단·온보딩
status: review
type: feature
depends_on: ["P03-T01"]
parallel_group: ""
owner: "Codex/p03_t02"
started_at: "2026-08-07T23:04:22+09:00"
blocked_reason: ""
owned_files: ["src/features/profile/nickname.ts", "src/features/profile/nickname.test.ts", "src/app/onboarding/page.tsx", "src/app/onboarding/page.test.tsx", "src/app/onboarding/form.tsx", "src/app/onboarding/actions.ts", "src/app/onboarding/actions.test.ts", "supabase/migrations/202608070004_profile_nickname_constraint.sql", "supabase/tests/profile_nickname.sql"]
shared_files: ["src/types/database.ts"]
implementation_commit: "43d790c"
reviewer: ""
review_commit: ""
---

# 목표

별명 검증·중복 차단·온보딩을 완료해 다음 의존 작업이 사용할 검증된 산출물을 제공한다.

# 범위

- 생성 또는 수정: src/features/profile/nickname.ts
- 생성 또는 수정: src/features/profile/nickname.test.ts
- 생성 또는 수정: src/app/onboarding/page.tsx
- 생성 또는 수정: src/app/onboarding/page.test.tsx
- 생성 또는 수정: src/app/onboarding/form.tsx
- 생성 또는 수정: src/app/onboarding/actions.ts
- 생성 또는 수정: src/app/onboarding/actions.test.ts
- 생성 또는 수정: supabase/migrations/202608070004_profile_nickname_constraint.sql
- 생성 또는 수정: supabase/tests/profile_nickname.sql
- 통합 소유 시에만 수정: src/types/database.ts

# 제외

제품 설계의 명시적 제외 기능과 이 카드 뒤 작업의 기능은 추가하지 않는다.

# 선행조건과 차단 해제

P03-T01가 모두 done이면 ready로 전환한다.

# 파일

- src/features/profile/nickname.ts
- src/features/profile/nickname.test.ts
- src/app/onboarding/page.tsx
- src/app/onboarding/page.test.tsx
- src/app/onboarding/form.tsx
- src/app/onboarding/actions.ts
- src/app/onboarding/actions.test.ts
- supabase/migrations/202608070004_profile_nickname_constraint.sql
- supabase/tests/profile_nickname.sql
- src/types/database.ts

# 인터페이스

선행 카드가 제공한 공개 타입과 동작만 소비한다. 이 카드의 파일과 검증 결과가 후속 카드의 계약이다.

# 실행 순서

1. AGENTS.md와 이 카드를 읽고 ready 여부를 확인한 뒤 owner, started_at, status를 갱신한다.
2. 인수 조건을 가장 좁게 증명하는 실패 테스트를 owned_files의 테스트 경로에 먼저 작성한다.
3. 다음 작업 전 검사를 실행한다: npm run test -- src/features/profile/nickname.test.ts src/app/onboarding/actions.test.ts && supabase test db supabase/tests/profile_nickname.sql
4. 기대 결과를 확인한다: 별명 검증 함수·온보딩 액션과 DB CHECK 제약이 없어 실패.
5. 범위 안의 최소 변경만 구현한다.
6. 다음 통과 검사를 실행한다: npm run test -- src/features/profile/nickname.test.ts src/app/onboarding/actions.test.ts && supabase db reset && supabase test db && npm run typecheck
7. ./scripts/check-harness.sh와 사용 가능한 공통 완료 명령을 실행한다.
8. 카드와 대시보드를 review로 바꾸고 구현 커밋을 기록한다.
9. 구현자와 다른 리뷰어가 승인하면 reviewer, review_commit과 done 상태를 같은 커밋에 기록한다.

# 검증

- 증거 유형: RED/GREEN
- 작업 전 필수 행동: 실패 테스트를 먼저 작성한다.
- 작업 전 명령: npm run test -- src/features/profile/nickname.test.ts src/app/onboarding/actions.test.ts && supabase test db supabase/tests/profile_nickname.sql
- 예상 작업 전 결과: 별명 검증 함수·온보딩 액션과 DB CHECK 제약이 없어 실패
- 완료 명령: npm run test -- src/features/profile/nickname.test.ts src/app/onboarding/actions.test.ts && supabase db reset && supabase test db && npm run typecheck
- 기대 완료 결과: 2~20자 허용 문자, 인증된 사용자 ID 고정, 대소문자 무관 중복과 DB 직접 우회 거부가 통과한다.

# 인수 조건

- 2~20자 허용 문자와 대소문자 무관 중복 처리가 서버와 DB 직접 접근에서 통과한다.
- 정상 경로와 거부 경계가 검증된다.
- 비밀 값, 범위 밖 변경과 병렬 소유권 충돌이 없다.
- 카드, 대시보드, 구현 및 리뷰 커밋이 일치한다.

# 리뷰 증거

- RED: 별명 단위 테스트는 `./nickname` 모듈 부재로 실패했고, 액션 테스트는 `./actions` 모듈 부재로 실패했다. 보안 보강 테스트에서 성공 redirect 부재와 DB 예외 누출 3건, 페이지 테스트에서 클라이언트 폼 분리 모듈 부재를 확인했다.
- DB RED 설계: CHECK migration보다 먼저 잘못된 별명의 직접 INSERT/UPDATE 거부 pgTAP을 작성했다. 최초 실행은 Docker 미기동으로 DB 동작까지 도달하지 못했으며, 선행 스키마·보안 리뷰에서 CHECK 부재와 우회 가능성을 확인한 뒤 migration을 구현했다.
- GREEN: 별명·액션·페이지 좁은 테스트 28개가 통과했고, 정확히 2/20자 허용·1/21자 거부, 인증 사용자 ID 고정, named unique 중복, 일반 오류 비노출과 비인증 GET redirect를 검증했다.
- DB GREEN: 사용자가 로컬 Docker 환경에서 `npx supabase --agent no db reset`과 `npx supabase --agent no test db`를 실행해 4개 파일, 142개 pgTAP 모두 통과한 `Result: PASS`를 확인했다.
- 공통 검사: `./scripts/check-harness.sh`, `npm run lint`, `npm run typecheck`, `npm run test`(77), `npm run test:e2e`(16), `npm run build`, `git diff --check`가 모두 통과했다.
- 구현 커밋: `43d790c` (별명 검증 `faf43bd`, 온보딩·CHECK `ced9bd9`, lint 경계 `799f7d4` 포함)
- 독립 리뷰: `Codex/p03_t02_contract`와 `Codex/p03_t02_security`가 서버 인증 가드, ID·role 신뢰 경계, 오류 비노출, CHECK·unique·authenticated JWT pgTAP을 검토해 Critical/Important 없음으로 정적 승인했고, 이후 실제 DB PASS 증거를 확인했다.

# 커밋

권장 메시지: 기능: 별명 검증·중복 차단·온보딩
