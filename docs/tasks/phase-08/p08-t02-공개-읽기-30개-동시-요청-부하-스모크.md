---
id: P08-T02
title: 공개 읽기 30개 동시 요청 부하 스모크
status: in_progress
type: feature
depends_on: ["P08-T01"]
parallel_group: "R-A"
owner: "Codex/p08_t02"
started_at: "2026-08-08T20:04:38+09:00"
blocked_reason: ""
owned_files: ["scripts/load-smoke.mjs", "scripts/load-smoke.test.mjs", "docs/verification/load-smoke.md"]
shared_files: []
implementation_commit: ""
reviewer: ""
review_commit: ""
---

# 목표

공개 읽기 30개 동시 요청 부하 스모크을 완료해 다음 의존 작업이 사용할 검증된 산출물을 제공한다.

# 범위

- 생성 또는 수정: scripts/load-smoke.mjs
- 생성 또는 수정: scripts/load-smoke.test.mjs
- 생성 또는 수정: docs/verification/load-smoke.md
- 공유 파일 수정 없음

# 제외

제품 설계의 명시적 제외 기능과 이 카드 뒤 작업의 기능은 추가하지 않는다.

# 선행조건과 차단 해제

P08-T01가 모두 done이면 ready로 전환한다.

# 파일

- scripts/load-smoke.mjs
- scripts/load-smoke.test.mjs
- docs/verification/load-smoke.md

# 인터페이스

선행 카드가 제공한 공개 타입과 동작만 소비한다. 이 카드의 파일과 검증 결과가 후속 카드의 계약이다.

# 실행 순서

1. AGENTS.md와 이 카드를 읽고 ready 여부를 확인한 뒤 owner, started_at, status를 갱신한다.
2. 인수 조건을 가장 좁게 증명하는 실패 테스트를 owned_files의 테스트 경로에 먼저 작성한다.
3. 다음 작업 전 검사를 실행한다: node --test scripts/load-smoke.test.mjs
4. 기대 결과를 확인한다: 부하 실패 판정기가 없어 실패.
5. 범위 안의 최소 변경만 구현하고 별도 터미널에서 npm run dev로 로컬 서버를 시작한다.
6. 다음 통과 검사를 실행한다: node --test scripts/load-smoke.test.mjs && LOAD_BASE_URL=http://127.0.0.1:3000 LOAD_CONCURRENCY=30 LOAD_WARMUP=5 LOAD_ROUNDS=5 node scripts/load-smoke.mjs
7. ./scripts/check-harness.sh와 사용 가능한 공통 완료 명령을 실행한다.
8. 카드와 대시보드를 review로 바꾸고 구현 커밋을 기록한다.
9. 구현자와 다른 리뷰어가 승인하면 reviewer, review_commit과 done 상태를 같은 커밋에 기록한다.

# 검증

- 증거 유형: RED/GREEN
- 작업 전 필수 행동: 실패 테스트를 먼저 작성한다.
- 작업 전 명령: node --test scripts/load-smoke.test.mjs
- 예상 작업 전 결과: 부하 실패 판정기가 없어 실패
- 완료 명령: node --test scripts/load-smoke.test.mjs && LOAD_BASE_URL=http://127.0.0.1:3000 LOAD_CONCURRENCY=30 LOAD_WARMUP=5 LOAD_ROUNDS=5 node scripts/load-smoke.mjs
- 기대 완료 결과: 워밍업 뒤 공개 경로 30개 동시 요청 반복에서 HTTP 오류가 0건이다.

# 인수 조건

- 워밍업 뒤 공개 경로 30개 동시 요청 반복에서 HTTP 오류가 0건이다.
- 정상 경로와 거부 경계가 검증된다.
- 비밀 값, 범위 밖 변경과 병렬 소유권 충돌이 없다.
- 카드, 대시보드, 구현 및 리뷰 커밋이 일치한다.

# 리뷰 증거

작업 전 실패, 완료 명령 결과, 구현 커밋, 구현자와 다른 리뷰어, 승인 커밋을 이 절에 기록한다.

# 커밋

권장 메시지: 기능: 공개 읽기 30개 동시 요청 부하 스모크
